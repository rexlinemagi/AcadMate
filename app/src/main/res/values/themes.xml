<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.AcadMate" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/brand_primary_dark</item>
        <item name="colorOnPrimaryContainer">@color/white</item>
        <item name="windowActionBarOverlay">false</item>
        <item name="android:colorBackground">@color/bg_main</item>
        <item name="colorSurface">@color/bg_surface</item>
        <item name="android:statusBarColor">@color/brand_primary_dark</item>
        <item name="android:navigationBarColor">@color/bg_surface</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@color/bg_main</item>
    </style>

    <style name="Theme.AcadMate" parent="Base.Theme.AcadMate" />

    <style name="Theme.AcadMate.Main" parent="Base.Theme.AcadMate">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>